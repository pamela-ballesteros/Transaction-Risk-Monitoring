
# OpenAI API Key — used for ModerationMiddleware (content screening of notes).
# If omitted, the system falls back to heuristic moderation automatically.
OPENAI_API_KEY=sk-...your-key-here...

# Optional: set log output directory (default: ./logs)
LOG_DIR=logs

# Optional: auto-approve HITL in non-interactive environments (true/false)
AUTO_APPROVE_HITL=false
